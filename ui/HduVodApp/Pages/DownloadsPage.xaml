<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="HduVodApp.Pages.DownloadsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:svc="using:HduVodApp.Services"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    NavigationCacheMode="Required"
    mc:Ignorable="d">

    <Grid Padding="20" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="下载列表"
                   Style="{StaticResource TitleTextBlockStyle}" />

        <Grid Grid.Row="1">
            <TextBlock x:Name="EmptyHint"
                       Text="暂无下载记录。在「课程浏览」里选择录像机位即可开始下载。"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                       HorizontalAlignment="Center" VerticalAlignment="Center" />

            <ListView x:Name="TaskList" SelectionMode="None">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="svc:DownloadItem">
                        <Border Background="{ThemeResource LayerFillColorDefaultBrush}"
                                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                BorderThickness="1" CornerRadius="8"
                                Padding="14" Margin="0,4">
                            <Grid ColumnSpacing="12" RowSpacing="6">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>

                                <TextBlock Grid.Row="0" Grid.Column="0"
                                           Text="{x:Bind FileName}"
                                           TextTrimming="CharacterEllipsis"
                                           Style="{StaticResource BodyStrongTextBlockStyle}" />

                                <TextBlock Grid.Row="1" Grid.Column="0"
                                           Text="{x:Bind MetaText}"
                                           FontSize="12"
                                           TextTrimming="CharacterEllipsis"
                                           Foreground="{ThemeResource TextFillColorSecondaryBrush}" />

                                <!-- 播放/暂停二合一 + 删除叉叉 -->
                                <StackPanel Grid.Row="0" Grid.Column="1" Grid.RowSpan="2"
                                            Orientation="Horizontal" Spacing="6"
                                            VerticalAlignment="Center">
                                    <Button Width="40" Height="40" Padding="0"
                                            ToolTipService.ToolTip="{x:Bind ToggleTip, Mode=OneWay}"
                                            Click="Toggle_Click" Tag="{x:Bind}">
                                        <FontIcon Glyph="{x:Bind ToggleGlyph, Mode=OneWay}" FontSize="16" />
                                    </Button>
                                    <Button Width="40" Height="40" Padding="0"
                                            ToolTipService.ToolTip="删除"
                                            Click="Delete_Click" Tag="{x:Bind}">
                                        <FontIcon Glyph="&#xE711;" FontSize="16" />
                                    </Button>
                                </StackPanel>

                                <ProgressBar Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2"
                                             Value="{x:Bind Progress, Mode=OneWay}"
                                             Maximum="100" />

                                <StackPanel Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2"
                                            Orientation="Horizontal" Spacing="16">
                                    <TextBlock Text="{x:Bind StateText, Mode=OneWay}"
                                               FontSize="12"
                                               Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}" />
                                    <TextBlock Text="{x:Bind SizeText, Mode=OneWay}"
                                               FontSize="12"
                                               Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                                    <TextBlock Text="{x:Bind SpeedText, Mode=OneWay}"
                                               FontSize="12"
                                               Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                                    <TextBlock Text="{x:Bind Error, Mode=OneWay}"
                                               FontSize="12"
                                               Foreground="{ThemeResource SystemFillColorCriticalBrush}"
                                               TextWrapping="Wrap" />
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
    </Grid>
</Page>
