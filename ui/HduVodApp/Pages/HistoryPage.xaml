<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="HduVodApp.Pages.HistoryPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:svc="using:HduVodApp.Services"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    NavigationCacheMode="Enabled"
    mc:Ignorable="d">

    <Grid Padding="20" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock Text="下载历史" Style="{StaticResource TitleTextBlockStyle}" />
                <TextBlock x:Name="SubText"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                           FontSize="12" />
            </StackPanel>
            <Button Grid.Column="1" Content="刷新" Click="Refresh_Click"
                    VerticalAlignment="Center" />
        </Grid>

        <Grid Grid.Row="1">
            <TextBlock x:Name="EmptyHint"
                       Text="当前账号还没有下载历史记录。"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                       HorizontalAlignment="Center" VerticalAlignment="Center" />

            <ListView x:Name="HistoryList" SelectionMode="None">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="svc:HistoryRecord">
                        <Border Background="{ThemeResource LayerFillColorDefaultBrush}"
                                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                BorderThickness="1" CornerRadius="8"
                                Padding="14" Margin="0,4">
                            <Grid ColumnSpacing="12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="170" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="{x:Bind DateTimeText}"
                                               Style="{StaticResource BodyStrongTextBlockStyle}"
                                               TextWrapping="Wrap" />
                                    <TextBlock Text="{x:Bind StatusText}" FontSize="12"
                                               Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}" />
                                </StackPanel>

                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="{x:Bind Course}" TextWrapping="Wrap" />
                                    <TextBlock Text="{x:Bind FileName}" FontSize="12"
                                               TextTrimming="CharacterEllipsis"
                                               Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                                </StackPanel>

                                <TextBlock Grid.Column="2" Text="{x:Bind SizeText}"
                                           VerticalAlignment="Center" FontSize="12"
                                           Foreground="{ThemeResource TextFillColorSecondaryBrush}" />

                                <StackPanel Grid.Column="3" Orientation="Horizontal" Spacing="6"
                                            VerticalAlignment="Center">
                                    <Button ToolTipService.ToolTip="复制直链"
                                            Click="Copy_Click" Tag="{x:Bind}">
                                        <FontIcon Glyph="&#xE8C8;" FontSize="13" />
                                    </Button>
                                    <Button ToolTipService.ToolTip="重新下载"
                                            Click="Redownload_Click" Tag="{x:Bind}">
                                        <FontIcon Glyph="&#xE777;" FontSize="13" />
                                    </Button>
                                    <Button ToolTipService.ToolTip="删除该记录"
                                            Click="Delete_Click" Tag="{x:Bind}">
                                        <FontIcon Glyph="&#xE74D;" FontSize="13" />
                                    </Button>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
    </Grid>
</Page>
